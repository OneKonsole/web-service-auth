import * as chai from 'chai';
import chaiHttp from 'chai-http';
import { describe, it, afterEach } from 'mocha';

chai.use(chaiHttp);
const { expect } = chai;

describe('App', () => {
    // TODO.
});
